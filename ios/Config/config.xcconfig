PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = NativeApp
BUNDLE_IDENTIFIER = myapp.nativeapp

RUNTIME_URL = https:\/\/nativeapp295-sandbox.mxapps.io/

PRODUCT_NAME = NativeApp
